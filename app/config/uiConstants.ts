// Centralized UI constants for consistent styling across the application

/**
 * Scroll margin offset for anchor navigation sections.
 * This value ensures sections don't get hidden behind fixed headers when scrolling.
 * The Tailwind class `scroll-mt-20` corresponds to 5rem (80px).
 */
export const SCROLL_MARGIN_CLASS = 'scroll-mt-20';
